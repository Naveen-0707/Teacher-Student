# ── Core (required) ──────────────────────────────────────────────────────────
manim>=0.18.0          # animation engine (also needs a system FFmpeg w/ libass)
numpy>=1.24.0
Pillow>=10.0.0

# ── Voice / TTS ──────────────────────────────────────────────────────────────
edge-tts>=6.1.9        # default: natural online neural voices, no API key
pyttsx3>=2.90          # offline last-resort fallback

# ── Optional offline voices (install if you want fully-offline TTS) ────────────
# piper-tts>=1.2.0     # natural offline voice (run: generate.py setup)
# kokoro>=0.7.0        # alternative offline voice
# soundfile>=0.12.0    # required by kokoro to write audio

# NOTE: FFmpeg + ffprobe must be installed on the system PATH and built with
#       libass (for burned-in captions). Check with: python generate.py setup --check
